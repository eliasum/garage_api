# 9️⃣ ФАЙЛ __init__.py - ЧТО ЭТО?
# Файл с именем __init__.py делает папку "пакетом" Python
# Без этого файла Python не увидит schemas как модуль для импорта
# Двойное подчёркивание с двух сторон = "магическое" имя в Python
# Произносится как "дундер-инит" (от double underscore)

# 🔟 ИМПОРТЫ ВНУТРИ ПАКЕТА
from .part import PartBase, PartCreate, PartResponse

# from .part = "из файла part.py, который находится в этой же папке"
# Точка перед part = относительный импорт (текущая директория)
# import PartBase, PartCreate, PartResponse = импорт конкретных классов

# 1️⃣1️⃣ СПЕЦИАЛЬНАЯ ПЕРЕМЕННАЯ __all__
__all__ = ["PartBase", "PartCreate", "PartResponse"]
# __all__ = список имён, которые будут экспортированы при импорте через *
# Например: from app.schemas import *
# В этом случае импортируются только PartBase, PartCreate, PartResponse
# Квадратные скобки [] = создание списка (массива) в Python
# Кавычки вокруг каждого имени = строковое представление имени класса
# Запятые между элементами = разделители в списке
