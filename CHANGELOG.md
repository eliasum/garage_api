#2025.12.09 18:16 IMM

#Переход с чистого Python на FastAPI

📚 Содержание

    Основные различия подходов

    Ключевые концепции веб-API

    Практический переход на FastAPI

    Типичные вопросы и ответы

Основные различия подходов
🖥️ Чистый Python (консольный подход)

    Философия: Прямое выполнение инструкций сверху вниз

    Запуск: python main.py → выполнение → завершение

    Ввод данных: input() или аргументы командной строки

    Вывод данных: print() в консоль

    Состояние данных: В оперативной памяти, теряется при завершении

    Клиент: Только тот, кто запустил программу

    Аналог в C#: Console Application

🌐 FastAPI (веб-подход)

    Философия: Ожидание запросов и реакция на них

    Запуск: Сервер работает постоянно, слушает порт

    Ввод данных: HTTP-запросы (GET, POST, PUT, DELETE)

    Вывод данных: JSON в HTTP-ответах

    Состояние данных: Сохраняется между запросами (пока сервер работает)

    Клиент: Любой, кто может отправить HTTP-запрос

    Аналог в C#: ASP.NET Web API

🔄 Архитектурные различия

Консольный Python (последовательный):
text

Пользователь → Запуск программы → Выполнение кода → Вывод → Завершение

FastAPI (событийно-ориентированный):
text

Клиент → HTTP-запрос → FastAPI → Бизнес-логика → HTTP-ответ → Клиент
                    ↑                                   ↓
                    └───── Сервер работает постоянно ───┘

Ключевые концепции веб-API
📞 Что такое веб-API?

Веб-API — это как колл-центр для программ:

    URL = телефонный номер

    Эндпоинты = пункты меню ("нажмите 1 для...")

    Функции = операторы, которые отвечают на звонки

🎯 Основные компоненты
1. Эндпоинты (Endpoints)
python

@app.get("/health")  # Эндпоинт "здоровья" сервиса
def health_check():
    return {"status": "OK"}

    /health — проверка, что сервер работает

    /parts — получение списка запчастей

    /parts/{id} — получение конкретной запчасти

2. HTTP-методы

    GET — получить данные (как SELECT в SQL)

    POST — создать новые данные (как INSERT)

    PUT — обновить данные (как UPDATE)

    DELETE — удалить данные (как DELETE)

3. JSON — язык общения веб-API
python

# Python-словарь
{"name": "Масляный фильтр", "quantity": 5}

# Становится JSON
{
  "name": "Масляный фильтр",
  "quantity": 5
}

4. Слои приложения
text

┌───────────────────────┐
│     Веб-слой          │ ← FastAPI: HTTP ↔ Python
│   (Контроллеры)       │
├───────────────────────┤
│   Бизнес-слой         │ ← Твои классы Garage/Part
│   (Сервисы)           │
├───────────────────────┤
│   Слой данных         │ ← Будущая база данных
│   (Репозитории)       │
└───────────────────────┘

🔧 Технические термины простыми словами
Термин	Простое объяснение	Аналог в жизни
API-клиент	Что-то, что умеет "звонить" API	Телефон
Эндпоинт	Конкретный "номер" в API	Внутренний номер в колл-центре
HTTP-запрос	"Звонок" на сервер	Набрать номер телефона
JSON	Универсальный язык для данных	Английский как язык международного общения
Персистентность	Сохранение данных после выключения	Запись в блокнот вместо запоминания
Валидация	Проверка, что данные правильные	Проверка паспорта на границе
Роутер	Разделение эндпоинтов по файлам	Разные отделы в компании
Практический переход на FastAPI
🚀 Шаг 1: Минимальный рабочий пример
python

from fastapi import FastAPI
from typing import Dict, Any

app = FastAPI(title="Garage API")

# Твой существующий код (без изменений!)
class Garage:
    # ... твой класс Garage ...

garage = Garage()

# Добавляем веб-интерфейс
@app.get("/health")
def health_check() -> Dict[str, Any]:  # Аннотация типа для документации
    return {"status": "OK", "total_parts": len(garage.list_parts())}

🔍 Шаг 2: Исправление документации

Проблема: Swagger (документация) не генерируется
Решение: Добавить аннотации типов ко всем функциям
python

@app.get("/health")
def health_check() -> Dict[str, Any]:  # ← Этот тип ВАЖЕН!
    return {"status": "OK", "total_parts": len(garage.list_parts())}

📊 Шаг 3: Преобразование объектов в JSON
python

@app.get("/parts")
def list_parts() -> Dict[str, Any]:
    parts = garage.list_parts()
    
    # Преобразуем объекты Part в словари
    parts_list = []
    for part in parts:
        parts_list.append({
            "id": part.id,
            "name": part.name,
            "part_number": part.part_number,
            "quantity": part.quantity
        })
    
    return {"total": len(parts_list), "parts": parts_list}

🛡️ Шаг 4: Валидация с Pydantic
python

from pydantic import BaseModel

class PartCreate(BaseModel):
    name: str
    part_number: str
    quantity: int

@app.post("/parts")
def create_part(part_data: PartCreate):  # Автоматическая вадидация!
    # FastAPI проверяет, что данные правильные
    return garage.add_part(
        part_data.name, 
        part_data.part_number, 
        part_data.quantity
    )

Типичные вопросы и ответы
❓ "Зачем нужен /health эндпоинт?"

Ответ: Чтобы проверять, что сервер жив и работает. Используется:

    Мониторинг системами

    Проверка перед отправкой важных запросов

    Быстрая диагностика

❓ "Кто может использовать мой API?"

Ответ: Любой, кто может отправить HTTP-запрос:

    Ты через браузер

    Другая программа через curl или код

    Мобильное приложение

    Другой сервер

❓ "Что такое Swagger/OpenAPI?"

Ответ: Автоматическая документация твоего API.

    http://127.0.0.1:8000/docs — интерактивная документация

    http://127.0.0.1:8000/redoc — альтернативный вид

    http://127.0.0.1:8000/openapi.json — машинно-читаемая спецификация

❓ "Как тестировать API?"
bash

# 1. Через браузер
http://127.0.0.1:8000/health

# 2. Через PowerShell (curl)
curl http://127.0.0.1:8000/health

# 3. Через Python
import requests
response = requests.get("http://127.0.0.1:8000/health")
print(response.json())

❓ "Что делать, если...?"
Проблема	Решение
Документация пустая	Добавить аннотации типов к функциям
Ошибка 422 при POST	Проверить Pydantic модель или данные
Данные теряются после перезапуска	Добавить базу данных
Много кода в одном файле	Разделить на роутеры (отдельные файлы)
📈 Эволюция проекта
Фаза 1: Консольное приложение ✅

    Бизнес-логика (Garage, Part)

    Локальное выполнение

    Данные в памяти

Фаза 2: Веб-API на FastAPI (текущая) 🚧

    Те же классы Garage/Part

    Веб-интерфейс через HTTP

    JSON для обмена данными

    Автоматическая документация

Фаза 3: Профессиональная структура (будущая) 📅
text

garage_api/
├── app/
│   ├── main.py          # Точка входа
│   ├── models.py        # Garage, Part
│   ├── schemas.py       # Pydantic модели
│   └── routers/
│       └── parts.py     # Эндпоинты для запчастей

Фаза 4: База данных (будущая) 🔮

    Сохранение данных между перезапусками

    Безопасность и надежность

    Сложные запросы к данным

💡 Ключевые выводы

    FastAPI ≠ замена Python — это дополнение, веб-слой поверх твоей логики

    Твой код не меняется — классы Garage и Part остаются как есть

    Веб-API = доступность — твоя программа теперь доступна из любого места

    JSON = универсальность — все языки понимают этот формат

    Документация автоматически — меньше писать, больше делать

Самый важный принцип:
Ты не переучиваешься, а учишься "оборачивать" свою существующую логику в веб-интерфейс. Это как научиться говорить по телефону, когда уже умеешь говорить вживую.