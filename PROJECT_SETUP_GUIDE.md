# Руководство по работе с проектом Garage API

Этот документ описывает процесс настройки среды разработки и основные рабочие команды для проекта.

## 🚀 Быстрый старт для нового разработчика

1.  **Предварительные требования:**
    - Установленный **Python 3.11+** (рекомендуется 3.13).
    - Установленный менеджер зависимостей **Poetry**. [Инструкция по установке](https://python-poetry.org/docs/#installation).

2.  **Получить код проекта:**
    ```bash
    git clone <URL_репозитория>
    cd garage_api
    ```

3.  **Установить зависимости проекта:**
    ```bash
    poetry install
    ```
    *Эта команда создаст виртуальное окружение `.venv` в папке проекта и установит в него все библиотеки, указанные в `pyproject.toml` и `poetry.lock`.*

4.  **Открыть проект в VS Code:**
    - Закройте и снова откройте папку `garage_api` в VS Code.
    - Редактор автоматически определит интерпретатор из `.venv`.
    - При открытии нового терминала (`Ctrl+``) окружение активируется автоматически (вы увидите `(garage-api-py3.13)` в начале строки).

5.  **Запустить сервер разработки:**
    ```bash
    # Способ 1 (если окружение уже активировано в терминале):
    uvicorn app.main:app --reload
    # Способ 2 (универсальный, через Poetry, работает всегда):
    poetry run uvicorn app.main:app --reload
    ```
    - Сервер будет доступен по адресу: [http://127.0.0.1:8000](http://127.0.0.1:8000)
    - Интерактивная документация API (Swagger): [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

## 📦 Основные команды Poetry

| Команда | Назначение |
| :--- | :--- |
| `poetry install` | Установить все зависимости проекта (основная команда после клонирования). |
| `poetry add <пакет>` | Добавить новый пакет в зависимости и установить его (напр., `poetry add requests`). |
| `poetry add --group dev <пакет>` | Добавить пакет только для разработки (напр., `pytest`, `black`). |
| `poetry update` | Обновить все пакеты до последних версий, разрешённых `pyproject.toml`. |
| `poetry shell` | Активировать виртуальное окружение проекта в новой оболочке терминала. |
| `poetry run <команда>` | Выполнить любую команду внутри виртуального окружения проекта. |
| `poetry env info --path` | Показать путь к текущему виртуальному окружению проекта. |
| `deactivate` | Выйти из активированного виртуального окружения (команда самого Python). |

## ⚙️ Настройка среды разработки (VS Code)

Для корректной работы в проекте уже создана папка `.vscode/` с настройками:

*   **`.vscode/settings.json`** — обеспечивает автоматический выбор интерпретатора и активацию виртуального окружения при запуске терминала.

Если вы открываете проект в другом редакторе, убедитесь, что он использует интерпретатор из `.\garage_api\.venv\Scripts\python.exe`.

## 📁 Структура проекта (кратко)
garage_api/
├── .venv/ # Виртуальное окружение (создаётся poetry install, не коммитится в Git)
├── .vscode/ # Настройки редактора VS Code
├── app/ # Исходный код приложения
│ ├── api/ # Эндпоинты API (роутеры)
│ ├── core/ # Основные настройки и конфигурации
│ ├── db/ # Модели и подключение к базе данных (SQLAlchemy)
│ ├── schemas/ # Pydantic-схемы для валидации данных
│ ├── services/ # Бизнес-логика, сервисный слой
│ └── main.py # Точка входа в приложение (создание экземпляра FastAPI)
├── tests/ # Модульные и интеграционные тесты
├── .gitignore # Список игнорируемых Git файлов
├── poetry.lock # Фиксированные версии всех зависимостей (коммитить обязательно!)
├── pyproject.toml # Конфигурация Poetry, зависимости проекта, метаданные
├── PROJECT_SETUP_GUIDE.md # Этот файл
└── README.md # Общее описание проекта


## 🚨 Устранение частых проблем

| Проблема | Вероятная причина | Решение |
| :--- | :--- | :--- |
| В VS Code подчёркиваются импорты (`fastapi`, `sqlalchemy`) | VS Code использует не то окружение. | Нажмите `Ctrl+Shift+P` -> выберите `Python: Select Interpreter` -> выберите `.\\.venv\\Scripts\\python.exe`. |
| Команда `poetry` не найдена. | Poetry не установлен или не добавлен в PATH. | Следуйте [официальной инструкции](https://python-poetry.org/docs/#installation). |
| При запуске `uvicorn` возникает `ModuleNotFoundError`. | Зависимости не установлены. | В корне проекта выполните `poetry install`. |
| После `git pull` появились новые зависимости. | Локальное окружение устарело. | В корне проекта выполните `poetry install`. |

## 🧪 Запуск тестов

```bash
# Если тесты используют pytest (будущая настройка)
poetry run pytest